name: Test NPM Repo

on:
  workflow_call:

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/test-action'

      - name: ⚡ Run test
        run: npm test


