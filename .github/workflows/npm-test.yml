name: Test NPM Repo

on:
  workflow_call:

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/npm-cache'

      - name: ⚡ Run test
        run: npm test


